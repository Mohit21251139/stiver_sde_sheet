# Write your MySQL query statement below
SELECT r.contest_id , ROUND((COUNT(r.user_id) * 100 ) / ( SELECT COUNT(*) FROM Users ),2) as percentage FROM register r group by r.contest_id order by percentage desc, contest_id asc